# Databricks NEMWEB Lab - Python Dependencies
# Install with: pip install -r requirements.txt

# Databricks SDK (for local development and IDE support)
databricks-sdk>=0.20.0

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0

# PySpark (for local testing - match your DBR version)
# Uncomment if testing locally without Databricks Connect
# pyspark>=3.5.0

# Local development (no Spark required)
duckdb>=0.9.0
pandas>=2.0.0
requests>=2.28.0
