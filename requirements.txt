# Databricks NEMWEB Lab - Python Dependencies
# Install with: pip install -r requirements.txt

# Databricks SDK (for local development and IDE support)
databricks-sdk>=0.20.0

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0

# PySpark + Iceberg (for local testing with Spark)
# Note: Python DataSource API requires Spark 4.0 (DBR 15.4+)
# pyspark==3.5.0
# pyiceberg[pyarrow,duckdb]>=0.5.0

# Local development (no Spark required)
duckdb>=0.9.0
pandas>=2.0.0
requests>=2.28.0
